package com.wikaba.ogapp.agent;

/**
 * <p>This package is meant to allow emulation of a user playing Ogame through the web browser.</p>
 *
 * <p>When modifying or adding code to this package, try to maintain keeping an eye towards
 * writing non-Android-specific code. That is, this package should be (relatively) easy to
 * extract into its own library (and indeed, this is a long-term, low-priority goal for this
 * package).</p>
 */